# Default credentials per vendor
# These are fallback credentials that will be tried if no specific credentials are provided

credentials:
  hirschmann:
    default_username: "admin"
    default_password: "admin"
    alternative_credentials:
      - username: "admin"
        password: "admin"
      - username: "manager"
        password: "manager"
  
  lantech:
    default_username: "admin"
    default_password: "admin"
    alternative_credentials:
      - username: "root"
        password: "root"
      - username: "lantech"
        password: "lantech"
  
  kontron:
    default_username: "admin"
    default_password: "admin" 
    alternative_credentials:
      - username: "root"
        password: "root"
      - username: "kontron"
        password: "kontron"
  
  nomad:
    default_username: "admin"
    default_password: "admin"
    alternative_credentials:
      - username: "root"
        password: "root"
      - username: "nomad"
        password: "nomad"

# SSH connection settings
ssh_settings:
  port: 22
  timeout: 30
  retry_attempts: 3
  retry_delay: 2
